var app = angular.module("app", []);

app.controller("AppCtrl", function () {
    var app = this;
    app.message= "guet isch abig mit bootstrap";
});

app.directive("hpsof", function () {
    return {

    }
});